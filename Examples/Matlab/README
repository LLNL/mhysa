-------------------------------
    MATLAB SCRIPTS
------------------------------

This folder has some MATLAB scripts which can be used to 
generate the input files for HyPar

+ Functions named 'Writexxx.m' are functions that write out the
  'xxx' input file for HyPar.

+ Functions named 'Readxxx.m' are functions are read in the files
  written out by HyPar

+ SetDefaults.m returns a bunch parameters with their default 
  values that can be used to generate HyPar input files.
